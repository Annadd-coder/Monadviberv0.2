{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]          // ← это единственная строка, которую нужно
    },
    "resolveJsonModule": true   // чтобы TS не ругался на импорт .json (полезно)
  }
}